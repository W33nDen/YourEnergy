# План реалізації проєкту "Your Energy" (Final Polish Phase)

Цей план оновлено для фіналізації проєкту згідно з макетами Figma (Light Theme)
та Технічними вимогами.

## 🔴 Етап 1: Глобальний Редизайн (UI System)

_Мета: Перевести сайт на світлу тему та виправити змінні._

- [ ] **1.1. CSS Variables (`src/css/base.css`):**
  - [ ] Встановити палітру Light Theme:
    - `--black`: `#242424` (Текст, Активні елементи).
    - `--white`: `#F4F4F4` (Фон сторінки).
    - `--card-bg`: `#FFFFFF` (Фон карток).
    - `--gray-text`: `rgba(36, 36, 36, 0.6)` (Другорядний текст).
    - `--border-color`: `rgba(36, 36, 36, 0.2)`.
  - [ ] Шрифт: `Montserrat`.

## 🟠 Етап 2: Верстка Головної Сторінки (Figma Match)

_Мета: Зробити структуру ідентичною до макету._

- [ ] **2.1. Hero Section (`src/partials/hero.html` + `home.css`):**

  - [ ] **HTML:** Додати тег `<img>` з картинкою (використати Unsplash:
        `https://images.unsplash.com/photo-1571019614242-c5c5dee9f50b...`).
  - [ ] **Layout:** Flexbox. Текст зліва (50%), Картинка справа (45%).
  - [ ] **Styles:** Заголовок великий (60px+), чорний. Фон секції
        прозорий/світлий.

- [ ] **2.2. Фільтри (`filters.js` + css):**

  - [ ] Кнопки: Прозорий фон, сірий бордер, чорний текст.
  - [ ] Active state: Чорний фон, білий текст.

- [ ] **2.3. Картки Вправ (`exercises.css`):**
  - [ ] **Container:** Білий фон (`#FFF`), `border-radius: 15px`, `box-shadow`
        (м'яка тінь, без чорного бордера).
  - [ ] **Typography:** Назва вправи — чорна, велика.
  - [ ] **Details Row:** Вирівняти в один рядок (`display: flex`).
    - Labels ("Burned calories"): Сірий колір, дрібний шрифт (12px).
    - Values ("325"): Чорний колір, жирний шрифт.

## 🟡 Етап 3: Компоненти та Навігація

- [ ] **3.1. Footer (`footer.css`):**

  - [ ] Прозорий фон.
  - [ ] Соцмережі: SVG іконки (чорні).
  - [ ] Форма підписки: Валідація Regex
        (`^[\w+(\.\w+)?]+@[a-zA-Z_]+?\.[a-zA-Z]{2,3}$`).

- [ ] **3.2. Пагінація:**

  - [ ] Стилі кнопок: Неактивні — прозорі з чорним текстом. Активна — чорна.

- [ ] **3.3. Favorites Page (`favorites.html`):**
  - [ ] Використати ті самі стилі карток, що і на Home.
  - [ ] Empty State: Текст по центру, чорний колір.

## 🟢 Етап 4: Технічна Валідація (QA Checklist)

_Мета: Відповідність технічному завданню._

- [ ] **4.1. Код:**

  - [ ] Видалити всі `console.log`.
  - [ ] Перевірити `camelCase` змінних.
  - [ ] Перевірити підключення `modern-normalize`.
  - [ ] Перевірити атрибути `aria-label` для іконок.

- [ ] **4.2. API & Data:**

  - [ ] Перевірити відповідність полів Swagger UI (`burnedCalories`, `bodyPart`,
        `target`).
  - [ ] Перевірити логіку Favorites (збереження в LocalStorage).

- [ ] **4.3. Build:**
  - [ ] `npm run build` без помилок.
